apiVersion: v1
kind: Service
metadata:
  name: nodejs20-sparta-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: nodejs20-sparta-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080